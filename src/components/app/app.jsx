import React from 'react';
import Main from '../main/main.jsx';

// eslint-disable-next-line react/prop-types
const App = ({promoData}) => {
  return <Main promoData={promoData}/>;
};

export default App;
